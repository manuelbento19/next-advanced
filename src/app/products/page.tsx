import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Products"
}

export default function Page() {
    return (
      <main>
        <h1>
          Page Products
        </h1>
      </main>
    );
}
